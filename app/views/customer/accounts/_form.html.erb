<%= simple_form_for [:customer,  @account], :url => customer_account_path, :html => {:class => 'form-horizontal'} do |f| %>
    <%= render :partial => "inputs", locals: {f: f } %>
    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>